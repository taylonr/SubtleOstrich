@model IEnumerable<SubtleOstrich.Logic.Activity>

@{
    ViewBag.Title = "Activity";
}

<h2>Activity</h2>

<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
    </tr>
}

</table>
